user=`whoami`                            # finds your username on uberspace
domain="entwicklung.review"              # the main-domain to use for creation of subs
mysql -e "DROP DATABASE ${user}_$1"
uberspace web domain del ${1}.${domain}
cd ~/webroot/
rm -rf ${1}.${domain}
